Frontend (Svelte + Vite) for Menu Visualizer

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8000` as configured in `vite.config.js`.
